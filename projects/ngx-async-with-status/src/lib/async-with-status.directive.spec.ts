import { AsyncWithStatusDirective } from './async-with-status.directive';

describe('AsyncWithStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncWithStatusDirective();
    expect(directive).toBeTruthy();
  });
});
